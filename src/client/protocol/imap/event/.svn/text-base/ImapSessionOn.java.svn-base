package client.protocol.imap.event;

import java.net.URI;

import client.core.model.Event;

public class ImapSessionOn extends Event{
	private URI mSessionUri = null;
	
	public ImapSessionOn(URI uri) {
		mSessionUri = uri;
	}
	
	public URI getURI() {
		return mSessionUri;
	}
}
